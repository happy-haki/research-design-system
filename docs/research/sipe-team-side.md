# Research: sipe-team/side

Source: https://github.com/sipe-team/side

## Observed signals

- Uses `pnpm-workspace.yaml` + monorepo package structure.
- Includes `.storybook`, `docs`, `packages`, and release metadata (`.changeset`).
- README emphasizes TypeScript, Storybook, Vitest, and component consistency.

## Applied takeaways

- Keep pnpm workspace + package boundaries.
- Keep docs and test workflows as first-class outputs.
- Use typed APIs and release-friendly structure from day one.
